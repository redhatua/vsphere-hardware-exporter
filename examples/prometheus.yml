scrape_configs:
  - job_name: vsphere-hardware
    scrape_interval: 5m # data is cached and refreshed hourly; scraping faster gains nothing
    scrape_timeout: 30s
    static_configs:
      - targets: ["vsphere-hardware-exporter:9877"]
